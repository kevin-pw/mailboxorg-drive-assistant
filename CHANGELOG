[v1.3.0]
- feat: Implemented use of 'xvfb-run -a' when running 'FreeFileSync' to prevent system tray icon from appearing on each remote polling event.

[v1.2.0]
- feat: Replaced periodic remote polling machanism with lightweight sentinel file touch to trigger sync.

[v1.1.0]
- feat: Added periodic remote polling feature to trigger sync when changes are detected on remote drive.

[v1.0.0]
- Initial commit of fully functional setup assistant.